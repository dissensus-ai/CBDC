# Supplemental diagnostic plan, 12 September 2026

This is a retrospective extension after inspecting all August results. It is not preregistered and does not restore confirmatory status to any August claim. Decisions below are fixed before these new diagnostic seeds run.

1. Reproduce every one of the 52 August independent-population replicates at its original seed. Compare raw records exactly, and change inference metadata only where the original H2 gate was bypassed.
2. Reproduce the first default full-ladder unit (seed 2026082051) at its original configuration. Compare metrics and selected hyperparameters exactly; wall time is not scientific data.
3. Fill the absent ladder label-permutation diagnostic: two newly generated worlds at each of the nine 5% surface regimes and the 1% and 3% default regimes, with all four frozen models and original nested tuning. Permute entity labels after generation/features and before folds/tuning. Original signal remains in features; shuffled labels are used consistently for training and evaluation. New seed base 2026091201, successive seeds for each of 22 units, permutation stream seed+999. Save AP, AUC, budget metrics, all chosen hyperparameters, and every failure. Do not redraw. This is a leakage diagnostic; two replications per world do not estimate a precise null distribution.
4. Re-run both PET-AML simulator configurations at --days 2 --seed 7, publishing every shared output field side by side. Retain only as an illustrative queue/eligibility simulation, not cryptographic timing or detection evidence.
5. Independently aggregate raw frozen records, paired tier and model contrasts, 90% pointwise Student-t intervals, and arithmetic identities. Record that the surface ran at 5% prevalence despite protocol text naming 3%; no retroactive amendment repairs this. The missing E2 factorial and default bootstrap are excluded from claimed completion.

All new files go to revision-2026-09-12/evidence. Original frozen records and protocols remain immutable.

## Compute-only execution amendment

On 12 September, eight completed control records were retained, the four-worker process tree was stopped, and the driver gained validated resume plus a worker-count option. Remaining unrecorded units were resumed at their original seeds with 12 workers. No scientific setting or planned unit changed; incomplete computations were repeated at the same seed. This is an execution change, not a protocol/estimand amendment.
