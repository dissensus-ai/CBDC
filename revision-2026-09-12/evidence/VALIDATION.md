# Validation record

- 52/52 independent-population replicates freshly generated with pinned libraries; all raw record dictionaries exactly equal to the archived August records, zero failures. These are reproducibility reruns of the same seeds, not new independent scientific replications.
- All 122 frozen ladder records independently aggregated; no exclusions, unique seeds, component-to-total AP/recall arithmetic checked.
- Four new gatekeeping regression checks, eleven existing endpoint tests, eleven existing lock tests, and two generator-audit tests passed. Logs are included.
- Both complete two-day seed-7 PET-AML amount configurations freshly rerun. All shared output summaries disclosed together.
- Seven retained references checked against primary sources; three conference/journal DOIs additionally verified via scite. Crossref rate-limited; no successful direct Crossref check is claimed.
- PDF figures generated from adjudicated JSON only; TeX tables generated by the same regenerator from immutable raw records. No manually copied result cells.
- Final checks are complete: 22/22 retrospective control worlds, all OK with unique seeds; the full original ladder record matches every scientific field after JSON normalization. Mean T4 AUCs: .492 linear, .502 additive, .493 forest, .494 boosted.

An independent agent reviewed the new manuscript against the code and independently recomputed all six 52-replicate component/full-block means and intervals, matching adjudicated JSON within 1e-10. It found no additional manuscript-level scientific blocker. Two public-code documentation overclaims were narrowed: features.py no longer says the oracle map is achieved by an implemented ZK linkage protocol; endpoint.py no longer generalizes observed saturation to every possible future replicate. Neither edit changes computation.

The default surface configuration is compared field-for-field with the original default generator after matching entity count and label, and passes. Historical equivalence-test docstrings are now explicitly marked as exploratory pilot material, not the current manuscript thesis.

Final PDF: 17 pages; main TeX source: 5,851 whitespace words; rendered PDF text: 6,707 whitespace words (including headings, tables, references, and page headers). Source archive contains 15 files. The archive was extracted into an empty directory and cold-built successfully; rendered text is exactly identical to the working-tree PDF, with zero undefined references/citations and zero overfull boxes. Title/author metadata checked; title page, model/surface page, and final control/prevalence page inspected visually.

The first ladder replay comparison failed only because an in-memory dataclass tuple differs from its JSON list representation. The saved replay already matched every model output and chosen hyperparameter. After comparing parsed JSON artifacts, all five fields (label,seed,config,gate,models) match exactly. The driver now normalizes before comparison. No refit or changed empirical value was used to repair this validator. The failed comparator and correction are retained in diagnostics.log.
