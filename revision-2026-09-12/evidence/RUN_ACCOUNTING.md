# Run accounting

## Original archived observations

- Independent-population experiment: 52 successful train/test pairs; preserved input `detection/confirmatory/results/confirmatory_summary.json`, lock `160e9a7762879f76ce43e7ae364b7da6b81310ac56863d19b26b48e13f2a60da`. Each pair has six model×tier fits. Actual likelihood/inference metadata is distinguished from the legacy mistaken H2 label.
- Model/regime experiment: 20 default + 80 surface + 20 prevalence + 2 positive-control world units = 122, each with four models and four scored tiers. No gate exclusions. These counts cannot be described as 122 independent repetitions of a single estimand.
- Pilot: 20 excluded worlds. Its resolution scan is used only to disclose budget selection and saturation, not pooled into the 52-replicate result.

## September execution

- Re-ran all 52 original independent-population pairs at their exact original seeds: identical raw records, zero failures. Not new independent evidence.
- Reaggregated all 122 archived ladder units independently with paired intervals and arithmetic checks. Only one full original ladder unit is re-generated as the code replay diagnostic; no claim of full 122-unit refitting.
- Added 22 new per-world label-permutation controls, two at each tested regime. All four models use original nested tuning. These are retrospective diagnostics, not original pre-specified outcomes. The scheduler started four workers, preserved eight completed records, stopped unfinished computations, and resumed with 12 workers. Completed records were neither redrawn nor rerun; uncompleted units retain their original scheduled seed.
- Reran two amount configurations of the PET-AML simulator at two days/seed 7. Each generates 40,000 transfers. No deployment experiment or measured cryptographic benchmark.

Final completion counts and hashes appear in `adjudicated-results.json`, `ladder-replay.json`, and `package-validation.json`; do not infer them from the plan alone.
